% Programa de Teste
Calculo de idade%
INIT
 cont is integer;
 media, idade, soma,altura is integer;
 begin
 cont := 5;
 soma := 0;
 do

 write("Altura:" );
 read (altura);
 soma := soma+altura;
 cont := cont - 1;

 while(cont > 0);
 media := soma / cont;
 write("Media: ");
 write (media);
STOP